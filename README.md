Esta es una Single Page Application (SPA). La navegación usa hash routing (#datos, #acerca) — la página nunca se recarga, así que el token de Google queda vivo en memoria sin importar cuántas veces navegues entre secciones.

Donde no se necesita back end, tiene login y todo es utilizando el Google Drive personal de cada user
